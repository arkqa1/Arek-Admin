<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <% title %>
    </title>
</head>

<body>
    <form method="post" action="/delete?t=<%= table %>&d=<%= database %>">
        <table>
            <% for(name in data[0]) { %>
                <th>
                    <%= name %>
                </th>
            <% } %>
            <% for(index in data) { %>
                <tr>
                    <% for(key in data[index]) { %>
                        <td>
                            <div class="wartosc">
                                <%= data[index][key] %>
                            </div>
                        </td>
                    <% } %>
                    <td>
                        <input type="checkbox" name="<%= index %>">
                    </td>
                </tr>
            <% } %>
        </table>
        <input type="submit" value="Insert">
    </form>
</body>

</html>