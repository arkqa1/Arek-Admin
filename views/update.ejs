<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <% title %>
    </title>
</head>

<body>
    <table>
        <% for(name in data[0]) { %>
            <th>
                <%= name %>
            </th>
            <% } %>
            <% for(element of data) { %>
                <tr>
                    <form method="post" action="/update?d=<%= database %>&t=<%= table %>">
                    <% for(key in element) { %>
                        <td>
                            <input type="text" name="<%= key %>" value="<%= element[key] %>">
                        </td>
                    <% } %>
                    <td>
                        <input type="submit" value="Insert">
                    </td>
                </tr>
            <% } %>
        </form>
    </table>
</body>

</html>